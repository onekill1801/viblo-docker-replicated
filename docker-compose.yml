version: "3.7"

services:
  runner:
    image: myoung34/github-runner:latest
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      RUNNER_SCOPE: repo
      RUNNER_NAME_PREFIX: myrunner
      LABELS: some-label
      REPO_URL: https://github.com/onekill1801/viblo-docker-replicated
      EPHEMERAL: 1
      ACCESS_TOKEN: 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
    deploy:
      replicas: 4
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.2'
          memory: 256M
